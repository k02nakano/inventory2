<h1>New device</h1>

<%= render 'form' %>

<%= form_for @device do |f| %>
  <%= f.label :cabinet_id %>
  <%= f.collection_select :cabinet_id, Cabinet.all, :id, :name %>
<% end %>

<%= link_to 'Back', devices_path %>
